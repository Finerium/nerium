# Restaurant Automation Agent assets

Sample input and output examples for marketplace listing preview.

Real production deployment includes additional runtime configuration, supplier registry schema, and POS integration adapters not bundled in this preview.
